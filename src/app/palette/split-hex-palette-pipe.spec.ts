import { SplitHexPalettePipe } from './split-hex-palette-pipe';

describe('SplitHexPalettePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitHexPalettePipe();
    expect(pipe).toBeTruthy();
  });
});
