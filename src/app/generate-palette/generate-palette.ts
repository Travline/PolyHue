import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'app-generate-palette',
  imports: [RouterLink],
  templateUrl: './generate-palette.html',
  styleUrl: './generate-palette.scss'
})
export class GeneratePalette {

}
